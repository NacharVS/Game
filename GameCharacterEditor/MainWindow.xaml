<Window x:Class="GameCharacterEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameCharacterEditor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Button HorizontalAlignment="Left" Height="53" Margin="662,72,0,0" VerticalAlignment="Top" Width="114" Click="Button_Click">
            <TextBlock x:Name="addBook" TextWrapping="Wrap" Text="Новый персонаж"/>
        </Button>
        <DataGrid x:Name="Table" HorizontalAlignment="Left" Height="284" Margin="22,103,0,0" VerticalAlignment="Top" Width="562" ItemsSource="{Binding}" AutoGenerateColumns="True" HorizontalGridLinesBrush="DarkGray"
    RowBackground="LightGray" AlternatingRowBackground="White" RenderTransformOrigin="0.471,0.676" SelectionChanged="Table_SelectionChanged">
            <DataGrid.Items>

            </DataGrid.Items>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя персонажа" Binding="{Binding Path=nameObject}"></DataGridTextColumn>
                <DataGridTextColumn Header="Расса" Binding="{Binding Path= Author}"></DataGridTextColumn>
                <DataGridTextColumn Header="Защита от магии" Binding="{Binding Path= Illustrator}"></DataGridTextColumn>
                <DataGridTextColumn Header="Ловкость" Binding="{Binding Path= Publisher}"></DataGridTextColumn>
                <DataGridTextColumn Header="Интелект" Binding="{Binding Path= bornYear}"></DataGridTextColumn>
                <DataGridTextColumn Header="Сила" Binding="{Binding Path= ownerName}"></DataGridTextColumn>
            </DataGrid.Columns>

        </DataGrid>
        
        <DataGrid x:Name="tableGameTable" HorizontalAlignment="Left" Height="284" Margin="22,103,0,0" VerticalAlignment="Top" Width="562" ItemsSource="{Binding}" AutoGenerateColumns="True" HorizontalGridLinesBrush="DarkGray"
    RowBackground="LightGray" AlternatingRowBackground="White" Visibility="Hidden">
            <DataGrid.Items>

            </DataGrid.Items>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя персонажа" Binding="{Binding Path=nameObject}"></DataGridTextColumn>
                <DataGridTextColumn Header="Расса" Binding="{Binding Path= Developer}"></DataGridTextColumn>
                <DataGridTextColumn Header="Защита от магии" Binding="{Binding Path= GamePlay}"></DataGridTextColumn>
                <DataGridTextColumn Header="Ловкость" Binding="{Binding Path= valuePeople}"></DataGridTextColumn>
                <DataGridTextColumn Header="Интелект" Binding="{Binding Path= ownerName}"></DataGridTextColumn>
                <DataGridTextColumn Header="Сила" Binding="{Binding Path= dateInLibrary}"></DataGridTextColumn>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Window>
