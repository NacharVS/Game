<Window x:Class="GameCharacterEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameCharacterEditor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Button HorizontalAlignment="Left" Height="53" Margin="662,72,0,0" VerticalAlignment="Top" Width="114" Click="Button_Click">
            <TextBlock x:Name="addPerson" TextWrapping="Wrap" Text="Новый персонаж"/>
        </Button>
        <DataGrid x:Name="PersonalTable" HorizontalAlignment="Left" Height="284" Margin="71,99,0,0" VerticalAlignment="Top" Width="464" ItemsSource="{Binding}" AutoGenerateColumns="True" HorizontalGridLinesBrush="DarkGray"
    RowBackground="LightGray" AlternatingRowBackground="White" RenderTransformOrigin="0.471,0.676" SelectionChanged="Table_SelectionChanged">
            <DataGrid.Items>

            </DataGrid.Items>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя персонажа" Binding="{Binding Path=nameObject}"></DataGridTextColumn>
                <DataGridTextColumn Header="Здоровье" Binding="{Binding Path= Health}"></DataGridTextColumn>
                <DataGridTextColumn Header="Расса" Binding="{Binding Path= Rassa}"></DataGridTextColumn>
                <DataGridTextColumn Header="Защита от магии" Binding="{Binding Path= ResistMagic}"></DataGridTextColumn>
                <DataGridTextColumn Header="Ловкость" Binding="{Binding Path= Lov}"></DataGridTextColumn>
                <DataGridTextColumn Header="Интелект" Binding="{Binding Path= Intelect}"></DataGridTextColumn>
                <DataGridTextColumn Header="Сила" Binding="{Binding Path= Strench}"></DataGridTextColumn>
            </DataGrid.Columns>

        </DataGrid>

        <DataGrid x:Name="tableGameTable" HorizontalAlignment="Left" Height="284" Margin="22,103,0,0" VerticalAlignment="Top" Width="562" ItemsSource="{Binding}" AutoGenerateColumns="True" HorizontalGridLinesBrush="DarkGray"
    RowBackground="LightGray" AlternatingRowBackground="White" Visibility="Hidden">
            <DataGrid.Items>

            </DataGrid.Items>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя персонажа" Binding="{Binding Path=nameObject}"></DataGridTextColumn>
                <DataGridTextColumn Header="Здоровье" Binding="{Binding Path= Health}"></DataGridTextColumn>
                <DataGridTextColumn Header="Расса" Binding="{Binding Path= Rassa}"></DataGridTextColumn>
                <DataGridTextColumn Header="Защита от магии" Binding="{Binding Path= ResistMagic}"></DataGridTextColumn>
                <DataGridTextColumn Header="Ловкость" Binding="{Binding Path= Lov}"></DataGridTextColumn>
                <DataGridTextColumn Header="Интелект" Binding="{Binding Path= Intelect}"></DataGridTextColumn>
                <DataGridTextColumn Header="Сила" Binding="{Binding Path= Strench}"></DataGridTextColumn>
            </DataGrid.Columns>

        </DataGrid>
        <Button x:Name="saveButton" Content="Сохранить" HorizontalAlignment="Left" Margin="662,245,0,0" VerticalAlignment="Top" Width="114" Click="saveButton_Click" Height="62"/>
    </Grid>
</Window>
